version: 2.1

orbs:
  maven: circleci/maven@1.1
  
jobs:
  build:
    docker: 
      - image: circleci/openjdk:8-jdk-stretch
    steps: 
      - checkout
      - run:
          name: Run tests
          command: mvn -f Editor/ clean install
      - list:
          name: List items
          command: ls -la
          
workflows:
  test:
    jobs:
      - build
      - list
